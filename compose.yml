services:
  web:
    image: ${DOCKER_USERNAME}/${PACKAGE_NAME}:latest
    container_name: "Talexia-Frontend"
    env_file:
      - .env.production
    ports:
      - 3000:3000
    restart: unless-stopped
